# AcademicAgent → Antigravity 嵌入式 Agent 转换指令

> **目标**：在另一台电脑上的 Antigravity 中，将 `/Users/jc/Desktop/code/AcademicAgent` 项目重构为 Antigravity 嵌入式 Agent。
>
> **使用方式**：让 Antigravity 读取本文件，然后说「请按照这个文件中的指令，逐步执行所有操作」。

---

## 变量定义

以下所有路径中，`$HOME` 指当前用户的 home 目录（通常为 `/Users/<username>`），`$PROJECT` 指项目根目录。

请在执行前确认：
- `$HOME` = 当前用户 home 目录（运行 `echo $HOME` 获取）
- `$PROJECT` = AcademicAgent 项目的绝对路径（默认 `/Users/jc/Desktop/code/AcademicAgent`）
- `$PYTHON` = Python 可执行文件名（`python` 或 `python3`）

> **如果路径与默认值不同**，请在下面所有文件内容中做全局替换：
> - `/Users/jc/Desktop/code/AcademicAgent` → 实际的 `$PROJECT` 路径
> - `~/.gemini` → 实际的 `$HOME/.gemini` 路径
> - `"command": "python"` → `"command": "$PYTHON"`

---

## 前置条件

```bash
# 1. 确认项目存在
ls $PROJECT/src/mcp_servers/paper_search.py

# 2. 安装 Python 依赖（如果没有 venv 就直接装）
cd $PROJECT && pip install -r requirements.txt
```

---

## 第 1 步：创建全局 GEMINI.md

**路径**：`$HOME/.gemini/GEMINI.md`

**作用**：定义 ScholarMind Agent 的角色、领域术语、工作规则。Antigravity 在每次对话中都会读取并遵守这些规则。

创建文件，**完整内容如下**：

```markdown
# ScholarMind — 通信感知领域学术研究 Agent

## 角色定义

你是一名通信感知（ISAC/6G）领域的高级学术研究助手，面向该领域的硕博研究生。

你需要：
- 深入理解论文中的信号处理、通信理论和感知算法
- 能识别论文中的核心贡献、方法创新点和实验设置
- 理解 OFDM、MIMO、波束成形、信道估计等核心概念
- 在回答中提供精确的数学表达和物理直觉
- 搜索论文时使用 MCP 工具（paper-search），确保每篇论文都有可验证引用

## 领域术语表

| 缩写 | 全称 | 中文 |
|:---|:---|:---|
| ISAC | Integrated Sensing and Communication | 通感一体化 |
| OFDM | Orthogonal Frequency Division Multiplexing | 正交频分复用 |
| MIMO | Multiple-Input Multiple-Output | 多输入多输出 |
| CRLB | Cramér-Rao Lower Bound | 克拉美罗下界 |
| DOA | Direction of Arrival | 到达方向 |
| RIS | Reconfigurable Intelligent Surface | 可重构智能超表面 |
| BER | Bit Error Rate | 误比特率 |
| SNR | Signal-to-Noise Ratio | 信噪比 |
| SLAM | Simultaneous Localization and Mapping | 同时定位与建图 |
| mmWave | Millimeter Wave | 毫米波 |

## 工作流程

1. 当用户请求搜索论文时，**必须调用 paper-search MCP 工具**，不要自行编造论文
2. 当用户上传/指定论文 PDF 时，使用 `paper_reader` Skill 解析（参见 `skills/paper_reader/SKILL.md`）
3. 对论文中的图表，使用 Skill 提取图片后结合多模态能力分析
4. 将论文中的关键信息通过 knowledge-graph MCP 工具写入知识图谱
5. 学习路径分析使用 `learning_path` Skill（参见 `skills/learning_path/SKILL.md`）
6. 代码仿真使用 code-execution MCP 工具

## 论文引用规则（重要！）

- **绝对禁止编造论文**：所有论文必须来自 MCP 搜索结果或用户提供的 PDF
- 如果不确定某篇论文是否存在，明确告知"我不确定这篇论文是否存在，建议搜索验证"
- 每个引用必须附带可验证的信息（DOI、arXiv ID 或 Semantic Scholar URL）

## 成本控制规则

- 单次任务最多搜索 3 轮论文，每轮最多 5 篇
- 论文引用链追踪最多 2 层深度
- 如果发现任务范围过大，先询问用户是否需要缩小范围

## 代码规范

- Python 代码遵循 PEP 8，使用 type hints
- 科学计算优先使用 numpy / scipy
- 通信仿真使用 numpy + scipy.signal

## 项目路径

- **项目根目录**: `/Users/jc/Desktop/code/AcademicAgent`
- **Skills**: `skills/paper_reader/`, `skills/learning_path/`
- **Workflows**: `.agents/workflows/`
- **MCP Servers**: `src/mcp_servers/` (paper_search, knowledge_graph, code_execution)
- **底层模块**: `src/core/`, `src/knowledge/`, `src/execution/`
- **Prompt 模板**: `prompts/domain_prompts/`
- **数据存储**: `data/` (知识图谱 JSON、沙箱输出)
```

---

## 第 2 步：注册 MCP Servers

**路径**：`$HOME/.gemini/antigravity/mcp_config.json`

**作用**：让 Antigravity 自动启动和调用 3 个 MCP 服务（论文搜索、知识图谱、代码执行）。

**覆盖写入**，**完整内容如下**：

```json
{
  "mcpServers": {
    "paper-search": {
      "command": "python",
      "args": ["/Users/jc/Desktop/code/AcademicAgent/src/mcp_servers/paper_search.py"],
      "env": {
        "PYTHONPATH": "/Users/jc/Desktop/code/AcademicAgent"
      }
    },
    "knowledge-graph": {
      "command": "python",
      "args": ["/Users/jc/Desktop/code/AcademicAgent/src/mcp_servers/knowledge_graph.py"],
      "env": {
        "PYTHONPATH": "/Users/jc/Desktop/code/AcademicAgent",
        "SCHOLARMIND_DATA_DIR": "/Users/jc/Desktop/code/AcademicAgent/data"
      }
    },
    "code-execution": {
      "command": "python",
      "args": ["/Users/jc/Desktop/code/AcademicAgent/src/mcp_servers/code_execution.py"],
      "env": {
        "PYTHONPATH": "/Users/jc/Desktop/code/AcademicAgent",
        "SCHOLARMIND_DATA_DIR": "/Users/jc/Desktop/code/AcademicAgent/data"
      }
    }
  }
}
```

> ⚠️ 如果系统只有 `python3` 而没有 `python`，把所有 `"command": "python"` 改为 `"command": "python3"`。

---

## 第 3 步：修改 5 个 MCP Server 源码

对 `$PROJECT/src/mcp_servers/` 下的 5 个 MCP Server 脚本添加 sys.path 注入和环境变量支持。

### 3.1 paper_search.py

在文件**最顶部**（第 1 行之前）插入以下代码：

```python
import sys
from pathlib import Path as _Path
_PROJECT_ROOT = _Path(__file__).parent.parent.parent
if str(_PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(_PROJECT_ROOT))

```

（后面紧跟原来的 `"""` 文档字符串开头）

### 3.2 paper_reader.py

同上，在文件**最顶部**插入完全相同的 6 行代码。

### 3.3 knowledge_graph.py

**修改 1**：在文件最顶部（`from __future__ import annotations` 之后）插入：

```python
import sys
from pathlib import Path as _Path
_PROJECT_ROOT = _Path(__file__).parent.parent.parent
if str(_PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(_PROJECT_ROOT))
```

**修改 2**：找到这一行：

```python
DATA_DIR = Path(__file__).parent.parent.parent / "data"
```

替换为：

```python
DATA_DIR = Path(os.getenv("SCHOLARMIND_DATA_DIR", str(Path(__file__).parent.parent.parent / "data")))
```

### 3.4 code_execution.py

与 3.3 完全相同的两处修改：
1. 在 `from __future__ import annotations` 之后插入 sys.path 注入代码
2. 将 `DATA_DIR = Path(__file__).parent.parent.parent / "data"` 替换为 `os.getenv` 版本

替换后：
```python
DATA_DIR = Path(os.getenv("SCHOLARMIND_DATA_DIR", str(Path(__file__).parent.parent.parent / "data")))
```

### 3.5 learning_path.py

**修改 1**：与 3.3 相同，在 `from __future__ import annotations` 之后插入 sys.path 注入代码

**修改 2**：在 `import logging` 之后添加 `import os`（如果没有的话）

**修改 3**：将 `DATA_DIR = Path(__file__).parent.parent.parent / "data"` 替换为 `os.getenv` 版本

---

## 第 4 步：创建 Skill — paper_reader

### 4.1 `$PROJECT/skills/paper_reader/SKILL.md`

创建文件，**完整内容如下**：

````markdown
---
name: paper_reader
description: Parse and analyze academic PDF papers — extract text, structure, images, and render pages for vision analysis
---

# Paper Reader Skill

## When to Use

Use this skill when the user:
- Provides a PDF paper and asks to "analyze", "read", or "summarize" it
- Wants to understand figures, tables, or equations in a paper
- Asks for the structure/sections of a paper
- Mentions a local PDF file path

## Prerequisites

- Python with PyMuPDF installed: `pip install PyMuPDF`
- Project root: `/Users/jc/Desktop/code/AcademicAgent`

## Instructions

### 1. Extract Paper Structure (Zero-cost, no API)

When user wants a quick overview of a paper's sections:

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action structure --pdf "<pdf_path>"
```

This returns JSON with section names and word counts. Present it as a formatted table.

### 2. Extract Full Text

When user wants the full text content:

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action text --pdf "<pdf_path>"
```

Returns the full text. Use it for knowledge extraction (pass to `add_paper_to_graph` MCP tool).

### 3. Extract Images from a Page

When user wants to analyze a specific figure:

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action images --pdf "<pdf_path>" --page <page_num>
```

This saves extracted images to `/tmp/scholarmind_images/` and returns their file paths. Use `view_file` on the saved image to analyze it with your built-in vision capability.

### 4. Render Entire Page as Image

When user wants full-page analysis (including formulas, layout, vector graphics):

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action render --pdf "<pdf_path>" --page <page_num> --dpi 200
```

Saves the rendered page to `/tmp/scholarmind_images/` and returns the path. Use `view_file` to see the rendered page and analyze it.

### 5. Get PDF Metadata

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action metadata --pdf "<pdf_path>"
```

Returns title, author, page count, file size, and whether it's a scanned PDF.

## Output Format

Always present results in structured markdown format:
- Metadata: as a table
- Structure: as a numbered list with word counts
- Images: show the image using `view_file`, then describe what you see
- For scanned PDFs: warn the user and suggest using page rendering + vision analysis

## Workflow Integration

After analyzing a paper, suggest next steps:
1. "要把这篇论文加入知识图谱吗？" → use `add_paper_to_graph` MCP tool
2. "需要分析某张具体图表吗？" → use image extraction + vision
3. "需要运行相关仿真吗？" → use `code-execution` MCP tools
````

### 4.2 `$PROJECT/skills/paper_reader/scripts/parse_pdf.py`

创建文件，**完整内容如下**：

```python
#!/usr/bin/env python3
"""
ScholarMind Paper Reader — CLI Wrapper
=======================================

CLI interface for PDF parsing, used by the Antigravity paper_reader Skill.

Usage:
    python parse_pdf.py --action text --pdf paper.pdf
    python parse_pdf.py --action structure --pdf paper.pdf
    python parse_pdf.py --action metadata --pdf paper.pdf
    python parse_pdf.py --action images --pdf paper.pdf --page 0
    python parse_pdf.py --action render --pdf paper.pdf --page 0 --dpi 200
"""

import argparse
import json
import sys
import os
from pathlib import Path

# Add project root to path (scripts/ -> paper_reader/ -> skills/ -> project root)
PROJECT_ROOT = Path(__file__).parent.parent.parent.parent
sys.path.insert(0, str(PROJECT_ROOT))

from src.core.pdf_parser import PDFParser
from src.core.multimodal import PaperStructureParser


OUTPUT_DIR = Path("/tmp/scholarmind_images")


def ensure_output_dir() -> Path:
    """Ensure output directory exists."""
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    return OUTPUT_DIR


def cmd_metadata(pdf_path: str) -> None:
    """Extract PDF metadata."""
    with PDFParser(pdf_path) as parser:
        meta = parser.get_metadata()
        is_scanned = parser.is_scanned_pdf()

        result = {
            "title": meta.title or "(未知)",
            "author": meta.author or "(未知)",
            "page_count": meta.page_count,
            "file_size_mb": meta.file_size_mb,
            "file_path": meta.file_path,
            "is_scanned": is_scanned,
        }
        print(json.dumps(result, ensure_ascii=False, indent=2))


def cmd_text(pdf_path: str) -> None:
    """Extract full text."""
    with PDFParser(pdf_path) as parser:
        text = parser.extract_full_text()
        # Output as JSON for structured parsing
        result = {
            "total_chars": len(text),
            "text": text,
        }
        print(json.dumps(result, ensure_ascii=False, indent=2))


def cmd_structure(pdf_path: str) -> None:
    """Extract paper section structure."""
    structure_parser = PaperStructureParser()

    with PDFParser(pdf_path) as parser:
        full_text = parser.extract_full_text()
        sections = structure_parser.parse_sections(full_text)

        result = {
            "section_count": len(sections),
            "sections": {
                name: {
                    "word_count": len(text.split()),
                    "char_count": len(text),
                    "preview": text[:300] + "..." if len(text) > 300 else text,
                }
                for name, text in sections.items()
            },
        }
        print(json.dumps(result, ensure_ascii=False, indent=2))


def cmd_images(pdf_path: str, page_num: int) -> None:
    """Extract images from a specific page and save to disk."""
    out_dir = ensure_output_dir()

    with PDFParser(pdf_path) as parser:
        images = parser.extract_images_from_page(page_num)

        if not images:
            print(json.dumps({
                "status": "no_images",
                "message": f"第 {page_num} 页没有嵌入图片。建议使用 --action render 进行整页渲染。",
            }, ensure_ascii=False, indent=2))
            return

        saved = []
        for img in images:
            import base64
            filename = f"page{page_num}_fig{img.index}.{img.format}"
            filepath = out_dir / filename
            img_bytes = base64.b64decode(img.base64_data)
            filepath.write_bytes(img_bytes)
            saved.append({
                "path": str(filepath),
                "format": img.format,
                "width": img.width,
                "height": img.height,
                "size_kb": img.size_kb,
            })

        print(json.dumps({
            "status": "ok",
            "page": page_num,
            "image_count": len(saved),
            "images": saved,
        }, ensure_ascii=False, indent=2))


def cmd_render(pdf_path: str, page_num: int, dpi: int) -> None:
    """Render a page as PNG image and save to disk."""
    out_dir = ensure_output_dir()

    with PDFParser(pdf_path) as parser:
        b64_data = parser.render_page_as_image(page_num, dpi=dpi)

        import base64
        filename = f"page{page_num}_rendered_dpi{dpi}.png"
        filepath = out_dir / filename
        img_bytes = base64.b64decode(b64_data)
        filepath.write_bytes(img_bytes)

        print(json.dumps({
            "status": "ok",
            "page": page_num,
            "dpi": dpi,
            "path": str(filepath),
            "size_kb": len(img_bytes) / 1024,
        }, ensure_ascii=False, indent=2))


def main():
    parser = argparse.ArgumentParser(
        description="ScholarMind Paper Reader CLI",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog="""
Actions:
  metadata   Extract PDF metadata (title, author, pages, scanned?)
  text       Extract full text content
  structure  Parse section structure with word counts
  images     Extract embedded images from a specific page
  render     Render entire page as PNG image for vision analysis
        """,
    )
    parser.add_argument(
        "--action",
        required=True,
        choices=["metadata", "text", "structure", "images", "render"],
        help="Action to perform",
    )
    parser.add_argument("--pdf", required=True, help="Path to PDF file")
    parser.add_argument("--page", type=int, default=0, help="Page number (0-indexed)")
    parser.add_argument("--dpi", type=int, default=200, help="DPI for page rendering (150/200/300)")

    args = parser.parse_args()

    try:
        if args.action == "metadata":
            cmd_metadata(args.pdf)
        elif args.action == "text":
            cmd_text(args.pdf)
        elif args.action == "structure":
            cmd_structure(args.pdf)
        elif args.action == "images":
            cmd_images(args.pdf, args.page)
        elif args.action == "render":
            cmd_render(args.pdf, args.page, args.dpi)
    except FileNotFoundError:
        print(json.dumps({
            "status": "error",
            "message": f"文件不存在: {args.pdf}",
        }, ensure_ascii=False, indent=2), file=sys.stderr)
        sys.exit(1)
    except Exception as e:
        print(json.dumps({
            "status": "error",
            "message": f"{type(e).__name__}: {e}",
        }, ensure_ascii=False, indent=2), file=sys.stderr)
        sys.exit(1)


if __name__ == "__main__":
    main()
```

---

## 第 5 步：创建 Skill — learning_path

### 5.1 `$PROJECT/skills/learning_path/SKILL.md`

创建文件，**完整内容如下**：

````markdown
---
name: learning_path
description: Analyze knowledge graph to detect knowledge gaps and generate personalized learning paths using PageRank and topological analysis
---

# Learning Path Skill

## When to Use

Use this skill when the user:
- Asks "我接下来应该学什么？" or "帮我规划学习路径"
- Asks "我哪些知识薄弱？" or "我还缺什么？"
- Asks "哪些概念最重要？" or "核心知识有哪些？"
- Wants to evaluate their knowledge graph health

## Prerequisites

- Knowledge graph must have data (use `get_graph_stats` MCP tool to check first)
- Project root: `/Users/jc/Desktop/code/AcademicAgent`

## Instructions

### 1. Generate Learning Path

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/learning_path/scripts/analyze_knowledge.py --action learning_path --focus "<optional_focus_area>" --max-items 15
```

Returns a complete learning path report including:
- Knowledge graph health metrics
- Knowledge gaps (sorted by severity)
- Recommended learning path (ordered by priority: critical → important → supplementary)

### 2. Detect Knowledge Gaps

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/learning_path/scripts/analyze_knowledge.py --action detect_gaps
```

Returns three types of gaps:
- 🔴 **foundation_gap**: Core concepts (high PageRank) with sparse attributes
- 🟡 **isolated_concept**: Concepts with degree ≤ 1 (disconnected knowledge)
- 🟠 **single_source**: Concepts known from only 1 paper (potential bias)

### 3. Get Concept Importance Ranking

```bash
cd /Users/jc/Desktop/code/AcademicAgent && python skills/learning_path/scripts/analyze_knowledge.py --action importance --top 10
```

Returns concepts ranked by composite score: `0.4×PageRank + 0.3×degree + 0.2×in_degree + 0.1×betweenness`

## Output Format

Present results using the markdown format returned by the scripts:
- Health metrics: as a table
- Gaps: with severity bars and actionable suggestions
- Learning path: as a numbered table with priority icons (🔴/🟡/🟢)

## Important Notes

- If the knowledge graph is empty, guide the user to first add papers using the `/paper-analysis` workflow
- If the graph has < 5 nodes, warn that results may be unreliable
- Always suggest specific next actions based on the gaps found
````

### 5.2 `$PROJECT/skills/learning_path/scripts/analyze_knowledge.py`

创建文件，**完整内容如下**：

```python
#!/usr/bin/env python3
"""
ScholarMind Learning Path — CLI Wrapper
=========================================

CLI interface for knowledge graph analysis, used by the Antigravity learning_path Skill.

Usage:
    python analyze_knowledge.py --action learning_path
    python analyze_knowledge.py --action learning_path --focus "beamforming"
    python analyze_knowledge.py --action detect_gaps
    python analyze_knowledge.py --action importance --top 10
"""

import argparse
import json
import sys
from pathlib import Path
from collections import Counter

# Add project root to path (scripts/ -> learning_path/ -> skills/ -> project root)
PROJECT_ROOT = Path(__file__).parent.parent.parent.parent
sys.path.insert(0, str(PROJECT_ROOT))

import os

DATA_DIR = Path(os.getenv("SCHOLARMIND_DATA_DIR", str(PROJECT_ROOT / "data")))
GRAPH_PATH = DATA_DIR / "knowledge_graph.json"

from src.knowledge.graph_store import KnowledgeGraphStore
from src.knowledge.graph_analyzer import KnowledgeGraphAnalyzer


def get_analyzer() -> tuple[KnowledgeGraphStore, KnowledgeGraphAnalyzer]:
    """Initialize graph store and analyzer."""
    store = KnowledgeGraphStore(graph_path=GRAPH_PATH)
    analyzer = KnowledgeGraphAnalyzer(store)
    return store, analyzer


def cmd_learning_path(focus: str, max_items: int) -> None:
    """Generate learning path."""
    store, analyzer = get_analyzer()

    if store.node_count == 0:
        print("📭 知识图谱为空，无法生成学习路径。")
        print("请先通过 knowledge-graph MCP 工具添加论文。")
        return

    result = analyzer.generate_learning_path(focus_area=focus, max_items=max_items)
    print(result.to_markdown())


def cmd_detect_gaps() -> None:
    """Detect knowledge gaps."""
    store, analyzer = get_analyzer()

    if store.node_count == 0:
        print("📭 知识图谱为空，无法检测盲区。")
        return

    gaps = analyzer.detect_knowledge_gaps()

    if not gaps:
        print(f"🎉 暂未发现明显知识盲区！")
        print(f"当前图谱: {store.node_count} 个节点, {store.edge_count} 个关系")
        return

    print(f"## ⚠️ 知识盲区检测报告\n")
    print(f"发现 **{len(gaps)}** 个知识盲区：\n")

    by_type: dict[str, list] = {}
    for gap in gaps:
        if gap.gap_type not in by_type:
            by_type[gap.gap_type] = []
        by_type[gap.gap_type].append(gap)

    type_names = {
        "foundation_gap": "🔴 基础概念缺失",
        "isolated_concept": "🟡 孤立概念",
        "single_source": "🟠 单源依赖",
    }

    for gap_type, gap_list in by_type.items():
        print(f"### {type_names.get(gap_type, gap_type)} ({len(gap_list)})\n")
        for gap in gap_list:
            severity_bar = "█" * int(gap.severity * 10) + "░" * (10 - int(gap.severity * 10))
            print(f"- **{gap.label}** ({gap.node_type})")
            print(f"  - 严重程度: [{severity_bar}] {gap.severity:.2f}")
            print(f"  - 原因: {gap.reason}")
            print(f"  - 📌 建议: {gap.suggested_action}\n")


def cmd_importance(top_n: int) -> None:
    """Get concept importance ranking."""
    store, analyzer = get_analyzer()

    if store.node_count == 0:
        print("📭 知识图谱为空，无法计算重要性。")
        return

    importance = analyzer.compute_importance()

    if not importance:
        print("知识图谱中没有可分析的节点。")
        return

    display_n = min(top_n, len(importance))
    print(f"## 🏆 概念重要性排名 (Top {display_n})\n")
    print("| 排名 | 概念 | 类型 | 综合评分 | PageRank | 度 | 入度 | 介数中心性 |")
    print("|:---|:---|:---|:---|:---|:---|:---|:---|")

    for i, imp in enumerate(importance[:top_n]):
        print(
            f"| {i + 1} | **{imp.label}** | {imp.node_type} | "
            f"{imp.importance_score:.3f} | {imp.pagerank:.4f} | "
            f"{imp.degree} | {imp.in_degree} | {imp.betweenness:.4f} |"
        )

    type_counts = Counter(imp.node_type for imp in importance[:top_n])
    print(f"\n**Top {top_n} 类型分布**: " + ", ".join(f"{t}: {c}" for t, c in type_counts.most_common()))


def main():
    parser = argparse.ArgumentParser(
        description="ScholarMind Learning Path CLI",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog="""
Actions:
  learning_path  Generate personalized learning path (with health, gaps, recommendations)
  detect_gaps    Detect knowledge gaps (foundation, isolated, single-source)
  importance     Get concept importance ranking (PageRank + centrality)
        """,
    )
    parser.add_argument(
        "--action",
        required=True,
        choices=["learning_path", "detect_gaps", "importance"],
        help="Action to perform",
    )
    parser.add_argument("--focus", default="", help="Focus area for learning path (e.g., 'beamforming')")
    parser.add_argument("--max-items", type=int, default=15, help="Max items in learning path")
    parser.add_argument("--top", type=int, default=10, help="Top N for importance ranking")

    args = parser.parse_args()

    try:
        if args.action == "learning_path":
            cmd_learning_path(args.focus, args.max_items)
        elif args.action == "detect_gaps":
            cmd_detect_gaps()
        elif args.action == "importance":
            cmd_importance(args.top)
    except FileNotFoundError as e:
        print(f"⚠️ 知识图谱文件不存在: {e}", file=sys.stderr)
        print("请先通过 knowledge-graph MCP 工具添加论文。")
        sys.exit(1)
    except Exception as e:
        print(f"⚠️ 分析失败: {type(e).__name__}: {e}", file=sys.stderr)
        sys.exit(1)


if __name__ == "__main__":
    main()
```

---

## 第 6 步：创建 3 个 Workflow

### 6.1 `$PROJECT/.agents/workflows/paper-analysis.md`

```markdown
---
description: End-to-end paper analysis — search, download, parse, extract knowledge, and generate learning suggestions
---

# Paper Analysis Workflow

Complete workflow for analyzing an academic paper from scratch.

## Steps

1. **Search for the paper** (if user doesn't have it locally):
   - Use the `paper-search` MCP tool: call `search_papers` with the topic
   - If user wants a specific paper, use `get_paper_details` with the Paper ID
   - Note the PDF link from the results

2. **Download the PDF** (if needed):
   ` ``bash
   curl -L -o /tmp/paper.pdf "<pdf_url>"
   ` ``

// turbo
3. **Get paper metadata and check if scanned**:
   ` ``bash
   cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action metadata --pdf "<pdf_path>"
   ` ``

// turbo
4. **Extract paper structure**:
   ` ``bash
   cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action structure --pdf "<pdf_path>"
   ` ``

5. **Present structure overview** to the user and ask which sections to focus on.

// turbo
6. **Extract full text** for knowledge graph:
   ` ``bash
   cd /Users/jc/Desktop/code/AcademicAgent && python skills/paper_reader/scripts/parse_pdf.py --action text --pdf "<pdf_path>"
   ` ``

7. **Analyze key figures** (if user requests):
   - Extract images: `--action images --page <N>`
   - Or render page: `--action render --page <N>`
   - Use `view_file` on the saved image to analyze it

8. **Add to knowledge graph**:
   - Use the `knowledge-graph` MCP tool: call `add_paper_to_graph` with the extracted text
   - Check the graph stats with `get_graph_stats`

9. **Suggest next steps**:
   - "需要分析更多图表吗？"
   - "要查看学习路径建议吗？" → suggest `/knowledge-build`
   - "要用仿真验证论文方法吗？" → suggest `/simulation`
```

> ⚠️ 注意：上面的 ` `` 应该写成没有空格的三个反引号。这里为了避免 markdown 嵌套解析问题而加了空格，实际创建文件时请去掉空格。

### 6.2 `$PROJECT/.agents/workflows/knowledge-build.md`

```markdown
---
description: Build knowledge graph from multiple papers, detect gaps, and generate personalized learning path
---

# Knowledge Build Workflow

Workflow for accumulating knowledge from multiple papers and analyzing learning needs.

## Steps

1. **Check current graph status**:
   - Use `knowledge-graph` MCP tool: call `get_graph_stats`
   - If empty, guide user to first analyze papers with `/paper-analysis`

2. **Batch add papers** (repeat for each paper):
   - If user has PDF: extract text using paper_reader skill, then `add_paper_to_graph`
   - If user has paper titles: use `paper-search` to find them first

3. **Review graph growth**:
   - Call `get_graph_stats` again to show progress
   - Call `query_knowledge` for key concepts

// turbo
4. **Run knowledge gap detection**:
   ` ``bash
   cd /Users/jc/Desktop/code/AcademicAgent && python skills/learning_path/scripts/analyze_knowledge.py --action detect_gaps
   ` ``

// turbo
5. **Generate learning path**:
   ` ``bash
   cd /Users/jc/Desktop/code/AcademicAgent && python skills/learning_path/scripts/analyze_knowledge.py --action learning_path
   ` ``

// turbo
6. **Get concept importance ranking**:
   ` ``bash
   cd /Users/jc/Desktop/code/AcademicAgent && python skills/learning_path/scripts/analyze_knowledge.py --action importance --top 10
   ` ``

7. **Present consolidated report**:
   - Graph health metrics
   - Top knowledge gaps with severity
   - Recommended learning path (critical → important → supplementary)
   - Suggested papers to read next (based on gaps)

8. **Suggest next actions**:
   - For foundation_gaps: "建议阅读综述论文补充基础"
   - For isolated_concepts: "建议寻找关联论文建立知识连接"
   - For single_source: "建议从其他论文交叉验证"
```

### 6.3 `$PROJECT/.agents/workflows/simulation.md`

```markdown
---
description: Run communication/signal processing simulations using templates or custom code
---

# Simulation Workflow

Workflow for running OFDM, MIMO, MUSIC, and custom simulations.

## Steps

1. **List available templates**:
   - Use `code-execution` MCP tool: call `list_code_templates`
   - Available: `ofdm_basic`, `mimo_beamforming`, `aoa_music`

2. **Choose template or write custom code**:
   - If user wants a template: use `explain_template` to show details and parameters
   - If user wants custom code: help write the Python code

3. **Modify parameters** (if using template):
   - Ask user which parameters to change
   - Format as: `"N_sc=128, CP_len=32"`

4. **Run simulation**:
   - Template: use `run_template(template_name, parameter_overrides)`
   - Custom: use `run_code(code, timeout, description)`

5. **Analyze results**:
   - Check stdout for numerical results
   - Check output_files for generated plots
   - Use `view_file` on any generated `.png` files to see and explain the plots

6. **Iterate if needed**:
   - "需要调整参数重新跑吗？"
   - "需要添加新的对比曲线吗？"
   - "要把仿真结果和论文理论值对比吗？"

7. **Connect to knowledge**:
   - If results relate to concepts in knowledge graph, suggest adding insights
   - "这个仿真结果验证了论文中的哪个结论？"
```

---

## 第 7 步：更新项目配置文件

### 7.1 `$PROJECT/CLAUDE.md` — 覆盖为简化版

```markdown
# ScholarMind - 学术研究 Agent

## 项目概述

ScholarMind 是面向通信感知（ISAC/6G）领域的多模态学术研究 Agent，已嵌入 Antigravity 框架。

## 架构概览

` ``
Antigravity (宿主 Agent)
├── ~/.gemini/GEMINI.md          ← 角色定义 + 领域规则
├── ~/.gemini/antigravity/
│   └── mcp_config.json          ← 3 个 MCP Server 注册
└── AcademicAgent/
    ├── skills/                   ← 2 个 Skill (paper_reader, learning_path)
    ├── .agents/workflows/        ← 3 个 Workflow (paper-analysis, knowledge-build, simulation)
    ├── src/mcp_servers/          ← 3 个 MCP Server (paper_search, knowledge_graph, code_execution)
    └── src/{core,knowledge,execution}/  ← 底层模块
` ``

## 快速使用

- **搜索论文**: 直接说 "帮我搜索 ISAC channel estimation 的论文"
- **分析论文**: 说 "帮我分析这篇 PDF" 或使用 `/paper-analysis`
- **知识积累**: 使用 `/knowledge-build` 工作流
- **仿真实验**: 使用 `/simulation` 工作流

## 详细文档

- 角色与规则: `~/.gemini/GEMINI.md`
- Skills: `skills/paper_reader/SKILL.md` , `skills/learning_path/SKILL.md`
- Workflows: `.agents/workflows/*.md`
- 项目文档: `docs/`

## 代码规范

- Python 代码遵循 PEP 8，使用 type hints
- 科学计算优先使用 numpy / scipy
- 通信仿真使用 numpy + scipy.signal
```

### 7.2 `$PROJECT/.gitignore` — 追加以下内容

在 `# ===== Claude Code =====` 段之后，添加：

```gitignore
# ===== Antigravity =====
.gemini/antigravity/
/tmp/scholarmind_images/
```

---

## 第 8 步：验证

执行以下命令验证一切就绪：

```bash
# 1. 语法检查 CLI 脚本
cd $PROJECT
python3 -m py_compile skills/paper_reader/scripts/parse_pdf.py && echo "✅ parse_pdf.py OK"
python3 -m py_compile skills/learning_path/scripts/analyze_knowledge.py && echo "✅ analyze_knowledge.py OK"

# 2. 验证 mcp_config.json 是合法 JSON
python3 -m json.tool ~/.gemini/antigravity/mcp_config.json > /dev/null && echo "✅ mcp_config.json OK"

# 3. 验证文件结构
echo "=== Skills ===" && ls -la skills/*/SKILL.md skills/*/scripts/*.py
echo "=== Workflows ===" && ls -la .agents/workflows/*.md
echo "=== GEMINI.md ===" && head -3 ~/.gemini/GEMINI.md

# 4. 验证 MCP Server 可导入（需要依赖已安装）
python3 -c "import src.mcp_servers.paper_search; print('✅ paper_search OK')"
python3 -c "import src.mcp_servers.knowledge_graph; print('✅ knowledge_graph OK')"
python3 -c "import src.mcp_servers.code_execution; print('✅ code_execution OK')"
```

---

## 最终目录结构

执行完所有步骤后，项目结构应该是：

```
AcademicAgent/
├── CLAUDE.md                           ← 简化版，指向 GEMINI.md
├── .agents/
│   └── workflows/
│       ├── paper-analysis.md           ← /paper-analysis 工作流
│       ├── knowledge-build.md          ← /knowledge-build 工作流
│       └── simulation.md              ← /simulation 工作流
├── skills/
│   ├── paper_reader/
│   │   ├── SKILL.md                    ← 论文阅读 Skill 定义
│   │   └── scripts/
│   │       └── parse_pdf.py            ← CLI 包装脚本
│   └── learning_path/
│       ├── SKILL.md                    ← 学习路径 Skill 定义
│       └── scripts/
│           └── analyze_knowledge.py    ← CLI 包装脚本
├── src/
│   ├── mcp_servers/                    ← 3 个 MCP Server（已修改 sys.path + env var）
│   ├── core/                           ← 底层模块（不变）
│   ├── knowledge/                      ← 底层模块（不变）
│   └── execution/                      ← 底层模块（不变）
├── prompts/                            ← Prompt 模板（不变）
├── tests/                              ← 测试（不变）
└── requirements.txt

~/.gemini/
├── GEMINI.md                           ← 角色定义 + 领域规则
└── antigravity/
    └── mcp_config.json                 ← 3 个 MCP Server 注册
```
